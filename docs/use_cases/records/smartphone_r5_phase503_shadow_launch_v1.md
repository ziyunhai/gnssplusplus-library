# Phase503 — masked-code shadow baseline replay launched

Build completed. Single native replay via run_phase503_code_monitor.py,
PID 3785650 / session 78090, presently running when recorded. Prelaunch
manifest pins raw inputs and current sources/binary, permits only the
fgo_problems.cpp shadow diagnostic delta against Phase495 pins, and removes
the main code floor from that candidate argv. NHC off; builder floor off;
numerical lambda floor on. No truth inputs or saved positions in inference.

Early aggregate: 2087 P-D masked rows reached geometry/quality eligibility;
807 pass the unchanged baseline median-centred residual test; zero unavailable
residuals, zero factors added, zero rows added to medians. This is the broad
masked pool, NOT intersection with the raw 426 outside-edge-supported rows.
Nor does it prove subsequent epoch retention for every shadow row.

Artifact directory output/smartphone-r5/phase503-h-code-monitor-v1/mtv-h/.
Output equality against Phase479 is asserted by launcher after completion,
not yet established. Poll existing handle; do not launch a duplicate based on
the presence of this record. Goal unmet, diagnostic-only, no accuracy claim.
